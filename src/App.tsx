// App.tsx is no longer used as the main component.
// The app entry point is now main.tsx which sets up routing.
// This file is kept for backwards compatibility but can be safely deleted.

export function App() {
  return null;
}
