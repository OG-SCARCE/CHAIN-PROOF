# 10‑Minute Video Walkthrough Script for ChainProof

**[0:00 – 0:30] Intro**
- "Hi, I’m Antigravity, your AI coding assistant. Today I’ll give you a comprehensive tour of the ChainProof web application – a premium‑grade, browser‑native evidence‑management platform. We’ll explore every page, component, and the latest gradient‑card animation, and see how they all fit together."

**[0:30 – 1:30] High‑Level Architecture**
- Mention tech stack: React 19, Vite, TailwindCSS, Framer Motion, TypeScript.
- Explain that all hashing and verification happen client‑side using the Web Crypto API, ensuring zero‑cloud dependency.
- Show folder structure: `src/components`, `src/pages`, `src/context`, `src/main.tsx`.

**[1:30 – 2:30] Main Entry – `main.tsx`**
- Highlight ReactDOM rendering, BrowserRouter, and global providers (`AuthContext`, `EvidenceContext`).
- Note the use of `StrictMode` and Vite’s hot‑module replacement.

**[2:30 – 4:00] Home Page – `HomePage.tsx`**
- Walk through hero section: animated `FaultyTerminal`, headline, and the new **gradient‑card steps**.
- Explain the `TerminalTextBlock` component (typing effect) and its props `delay` and `typingSpeed`.
- Show the feature grid built with `MagicBento` – explain star, spotlight, and tilt effects.
- Discuss the metrics row (`Metric` components) displaying evidence, cases, transfers, etc.
- Highlight the `ReflectiveCard` showing user profile when authenticated.
- Mention the `CurvedLoop` marquee divider.

**[4:00 – 5:00] Steps Section – Gradient Card Animation**
- Show the three cards (Capture & Seal, Transfer & Track, Verify & Alert).
- Explain the Tailwind classes:
  - `bg-gradient` via inline `style` for the outer card.
  - Inner dark card with `bg-[#1a1a1a]`.
  - Hover effects: `group-hover:scale-[0.98]`, `hover:shadow-[0_0_30px_1px_rgba(0,255,117,0.30)]`.
- Mention the `group` utility for coordinated hover states.

**[5:00 – 6:00] Page – Evidence Intake (`/intake`)**
- Describe file upload component, hash calculation using `crypto.subtle.digest`, and display of the sealed hash.
- Note UI elements: progress bar, copy‑to‑clipboard button.

**[6:00 – 7:00] Page – Custody Transfer (`/transfer`)**
- Explain the form to log hand‑offs: actor, recipient, location, notes.
- Show the transfer log table with timestamps.
- Mention state management via `EvidenceContext`.

**[7:00 – 8:00] Page – Verification (`/verify`)**
- Walk through re‑hashing a selected evidence file.
- Show the alert component that flashes red when a mismatch occurs.
- Highlight the `Alert` icon from `lucide-react`.

**[8:00 – 8:30] Page – Evidence Registry (`/registry`)**
- Explain search, filter, and export functionality.
- Show the table with sortable columns and pagination.

**[8:30 – 9:00] Page – Recent Activity (`/activity`)**
- Timeline view of all events: uploads, transfers, verifications.
- Use of `motion` for fade‑in animations.

**[9:00 – 9:30] Auth Flow – Login & Register**
- Briefly show the login form, validation, and token handling.
- Mention the `ReflectiveCard` fallback for unauthenticated users.

**[9:30 – 10:00] Closing**
- Recap the premium UI: glass‑morphism, micro‑animations, gradient cards.
- Invite viewers to explore the repo, star the project, and contribute.
- End with a call‑to‑action: "Check the README for setup instructions and start securing digital evidence today!"

---

*All timestamps are approximate; adjust pacing during recording.*

*Generated by Antigravity – your AI coding assistant.*
